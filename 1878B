#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t;
    if (!(cin >> t)) return 0;
    while (t--) {
        int n;
        cin >> n;
        for (int i = 1; i <= n; ++i) {
            if (i > 1) cout << ' ';
            cout << (2*i - 1);
        }
        cout << '\n';
    }
    return 0;
}
