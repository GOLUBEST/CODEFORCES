#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int a1,a2,a3,a4,a5,a6,a7;
        cin >> a1 >> a2 >> a3 >> a4 >> a5 >> a6 >> a7;
        
        long long sum = 0;
        
        vector<int> arr;
        arr.push_back(a1);
        arr.push_back(a2);
        arr.push_back(a3);
        arr.push_back(a4);
        arr.push_back(a5);
        arr.push_back(a6);
        arr.push_back(a7);
        
        sort(arr.begin(),arr.end());
        
        for(int i = 0 ; i < 6 ; i++) {
            sum += -(arr[i]);
        }
        
        sum += arr[6];
        
        cout << sum << endl;
    }
    return 0;
}
