#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n;
        cin >> n;
        
        vector<int> h;
        for(int i = 0 ; i < n ; i++) {
            int a;
            cin >> a;
            h.push_back(a);
        }
        
        int x = *max_element(h.begin() , h.end());
        int y = *min_element(h.begin() , h.end());
        
        cout << abs(x-y) + 1 << endl;
    }
}
