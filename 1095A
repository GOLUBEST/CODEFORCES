#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    string s;
    cin >> s;

    string ans = "";

    int i = 0 , j = 1;
    while(i < n) {
        ans.push_back(s[i]);
        j++;
        i = i + j;
    }

    cout << ans << endl;
}
